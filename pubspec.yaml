name: checkmate_hero
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  http: ^1.2.2
  archive: ^4.0.7
  crypto: ^3.0.3
  path_provider: ^2.1.4
  shared_preferences: ^2.3.2
  
  # For chess logic (move validation, game state)
  chess: ^0.8.1
  
  # For the chess board UI
  chessground: ^9.0.0
  
  # For state management
  provider: ^6.1.5+1
  
  # For the chess engine
  stockfish_chess_engine: ^0.8.2

  # Immutable collections used by chessground ValidMoves API
  fast_immutable_collections: ^11.1.0
  
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    # Declare Stockfish assets for the engine
    - assets/stockfish/
    - assets/puzzles.json


  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
